function toTimes(){
	window.location = "/";
}

function toCompass(){
	window.location = "/qibla";
}

function toSettings(){
	window.location = "/settings";
}

module.exports = {
    toTimes: toTimes,
    toCompass: toCompass,
    toSettings: toSettings
};
